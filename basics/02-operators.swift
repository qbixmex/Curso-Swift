// Operadores Aritméticos

let a: Int8 = 8;
let b: Int8 = 2;

print("\(a) + \(b) = \(a + b)")
print("\(a) - \(b) = \(a - b)")
print("\(a) x \(b) = \(a * b)")
print("\(a) / \(b) = \(a / b)")

// Modulo
print("9 % 3 = \(9 % 3)")
print("10 % 2 = \(10 % 2)")

// Operadores de Asignación
var counter = 0;

counter = counter + 1;
counter += 1;
counter -= 1;
counter *= 2;
counter /= 2;
counter %= 2;
